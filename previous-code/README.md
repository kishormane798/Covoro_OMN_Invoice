# Previous field-validation spec (archived)

This folder is **not** on Playwright’s `testDir` (`./tests`). `npx playwright test` and `npm run test:covoro` do not run these files.

Live spec: `tests/OMN_FieldValidation_CovoroTemplate_Test.spec.ts`  
Live Excel builder: `Helpers/omanFieldValidationExcelHelper.ts` (full Oman seed + current worker/buyer identity + one-field patch).

`OMN_FieldValidation_CovoroTemplate_Test.spec.ts` here is the previous empty-template `updateExcelField` path, kept for reference only.
