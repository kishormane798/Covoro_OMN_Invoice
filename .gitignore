# Dependencies
node_modules/

# Generated submit-combination workbooks (local only)
/submit/

# Test results
test-results/
playwright-report/
reports/
allure-results/
allure-report/

# Playwright / login outage markers (written at repo root during runs)
site-unavailable.json
site-unavailable-w*.json
consecutive-fail-skip.json
consecutive-fail-skip.lock
screenshots/
videos/
traces/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
# Cursor — ignore local workspace data; commit shared rules and skills
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/skills/
!.cursor/skills/**
/mcps/
/mcp/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]

# Build
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Generated invoice workbooks (Python clone output; templates stay in testData/uploads/)
/testData/generated/

# Test data / Playwright persisted auth (paths from repo root)
/storageState.json
/sessionStorage.json
/testData/auth/user.json
/cookies.json
/session.json

# Temporary files
tmp/
temp/
*.tmp

# Playwright
/playwright-report/
/blob-report/

# Local static report dump at repo root (not source)
/data/
/export/
/history/
/widgets/
/plugin/
/index.html
/app.js
/styles.css
/favicon.ico


/playwright/.cache/
/playwright/.auth/

# Local one-off data generation scripts (not framework code)
/scripts/covoro_batch/
/scripts/generate_bulk_submit_single_line.py
/scripts/generate_bulk_submit_multiline.py
/scripts/generate_bulk_submit_invoices.py
/scripts/generate_one_invoice_1000_items.py
/scripts/prepare_parallel_perf_excels.py

# Local-only Playwright specs (not part of shared suite)
/tests/KishorLocal/

# Local-only security upload fixtures (KishorLocal Security_Upload_Test only)
/testData/uploads/security/

# Local toolchain installs (e.g. bundled JDK) — not part of the suite
/.tools/

# Graphify (local knowledge graph; regenerate with `graphify update .`)
graphify-out/

# GitNexus (local code intelligence index; regenerate with `gitnexus analyze`)
.gitnexus/

# Local testcase notes / fixtures (not part of the shared suite)
/testcase/
